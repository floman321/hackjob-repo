<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".GuestJoinedNetwork$FileListFragment" >

    <TextView
        android:id="@+id/section_label"
        android:layout_width="wrap_content"
        android:textIsSelectable="true"
        android:layout_height="wrap_content" />
    
   <ListView
        android:id="@+id/lv_fragment_filelist"
        android:layout_width="fill_parent"
        android:layout_height="351dp" />

   <Button
       android:id="@+id/btn_syncfiles"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_alignLeft="@+id/lv_fragment_filelist"
       android:layout_below="@+id/lv_fragment_filelist"
       android:text="Sync Files with Server" 
       android:onClick="syncFiles"/>

   <Button
       android:id="@+id/btn_suggest"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_alignLeft="@+id/btn_syncfiles"
       android:layout_alignParentBottom="true"
       android:text="Suggest a file" />

</RelativeLayout>